-- INSERT INTO items (items, price)
-- VALUES('Small Desk', 100),('Large Desk', 300), ('Fan', 80);

-- SELECT * FROM items;

-- SELECT * FROM items WHERE price > 80;

SELECT * FROM items WHERE price <= 300;

-- had to delete some data this is why the id is starting on 7
